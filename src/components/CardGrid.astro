<ul role="list" class="card-grid">
  <slot />
</ul>

<style>
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
    gap: 1rem;
    padding: 0;
    margin-bottom: 3rem;
  }
</style>
